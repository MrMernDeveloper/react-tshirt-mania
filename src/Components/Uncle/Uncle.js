import React from 'react';

const Uncle = ({ house}) => {
    return (
        <div>
            <p><small>house:{ house}</small></p>
            <h4>Uncle</h4>
        </div>
    );
};

export default Uncle;